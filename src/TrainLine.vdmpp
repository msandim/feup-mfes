class TrainLine
types

values

instance variables
	private blocks: seq of Block := [ ];
	private trains: seq of Train := [ ];
	
	
operations
	
	-- TrainLine constructor
	public TrainLine: (seq of Block) ==> TrainLine
	TrainLine(initialBlocks) ==
	(
		blocks := initialBlocks;
		return self
	);

functions

traces

end TrainLine