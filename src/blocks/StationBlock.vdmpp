class StationBlock is subclass of Block
types

values

instance variables
	private block1: SimpleBlock;
	private block2: SimpleBlock;
	
operations

	-- constructor
	public StationBlock: Block * Block ==> StationBlock
	StationBlock(initialBlock1, initialBlock2) ==
	(
		block1 := initialBlock1;
		block2 := initialBlock2;
		return self
	);

functions

traces

end StationBlock